<!DOCTYPE html>
<html lang="en">
	<head>
	</head>
	<body>
		<article :class="tagsToEntryClass(tags)">
			<header webc:if="!hidePostTitle && title != undefined">
				<h2 class="p-name"><a class="p-url" rel="canonical" :href="url" @text="title"></a></h2>
				<small webc:if="summary" class="p-summary" @text="summary"></small>
			</header>
			<div class="e-content" @raw="content">
			</div>
			<footer>
				<a class="p-url" rel="canonical" :href="url">#</a>
				<date-time class="dt-published" :@date="date"></date-time>
				<a webc:for="tag of tags.filter(isPublicTag)" class="p-category" :href="`/post/tag/${slugify(tag)}`" @text="`#${tag}`"></a>
			</footer>
		</article>
	</body>
</html>
